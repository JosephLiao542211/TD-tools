{
  "name": "claude-chat",
  "version": "1.0.0",
  "description": "Claude Chat Application",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install"
  }
}
